// CaseId.cs  — 擴充地點（可重複使用）
public enum CaseId
{
    ForestEntrance,
    LeftPath,
    RightPath,
    Clearing,
    Faint,

    // 新增示例地點
    TownSquare,   // 小鎮廣場（常駐 HUB）
    DarkAlley,    // 黑暗小巷（重複遭遇）
    Inn           // 旅店（休息點）
}
